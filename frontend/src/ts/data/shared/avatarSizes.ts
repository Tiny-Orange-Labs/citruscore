type AvatarSizes = {
    large: number;
    medium: number;
    small: number;
};

const avatarSizes: AvatarSizes = Object.freeze({
    large: 224,
    medium: 128,
    small: 44,
});

export default avatarSizes;
